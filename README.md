# gha-indie-worker-lib-core

ORM and migration source of truth for Independent GitHub Actions worker and clone-server control plane. Web servers may use the read-only feature; only this crate (and declarative-migrations) apply writes/migrations.
